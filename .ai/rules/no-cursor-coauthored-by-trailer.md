# No Cursor Co-authored-by commit trailer

**Glob:** always (any commit)

When creating git commits in this repo, do **not** add:

```text
Co-authored-by: Cursor <cursoragent@cursor.com>
```

Do not pass that via `--trailer`, commit message body, or any other mechanism. Commit as the normal author only.
